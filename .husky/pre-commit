# .husky/pre-commit
oxfmt $(git diff --cached --name-only --diff-filter=ACMR | sed 's| |\\ |g') --no-error-on-unmatched-pattern
oxlint $(git diff --cached --name-only --diff-filter=ACMR | sed 's| |\\ |g') --fix
git update-index --again
